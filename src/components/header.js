import React from 'react'

function Header() {
    return (
        <header style={styles.header}>
            Lorem
        </header>
    )
}

export default Header

const styles ={
    header:{
    },
}